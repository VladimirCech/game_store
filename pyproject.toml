[tool.poetry]
name = "game-store"
version = "0.1.0"
description = ""
authors = ["Vladimir Cech <vladimir.cech@firma.seznam.cz>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
Django = "^5.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
